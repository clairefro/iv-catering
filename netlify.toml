[build]
  command = "npm run build"
  publish = "dist"

# Redirect .netlify.app to custom domain
[[redirects]]
  from = "https://*.netlify.app/*"
  to = "https://iovinos-catering.com/:splat"
  status = 301
  force = true